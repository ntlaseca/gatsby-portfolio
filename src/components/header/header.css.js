import styled from "styled-components";

export const Wrapper = styled.header`
  display: flex;
`

export const Logo = styled.div`
  & img {
    height: 7.2rem;

    &:hover {
      background: none;
    }
  }
`